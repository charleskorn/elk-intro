input {
  stdin {
    codec => json_lines
  }
}

filter {
  date {
    # 15/11/07 06:47:33.337253
    timezone => "UTC"
    match => ["time", "ISO8601"]
    remove_field => ["time"]
  }
}

output {
  stdout {
    codec => json_lines
  }
  elasticsearch {
    hosts => ["elasticsearch:9200"]
  }
}
